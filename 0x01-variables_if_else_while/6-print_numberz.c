#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */
int main(void)
{
int Dec;
for (Dec = 48 ; Dec <= 57 ; Dec++)
{
putchar(Dec);
}
putchar('\n');
return (0);
}
