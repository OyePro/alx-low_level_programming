#include "main.h"
/**
 * _isupper - functions to check for upper case in character c
 * @c: character that is checked
 *
 * Return: 0
 */
int _isupper(int c)
{
if (c >= 65 && c <= 90)
{
return (1);
}
else
{
return (0);
}
}
