#include "main.h"
/**
 * mul - functions to determine the multiplication of a and b
 * @a: the first integer
 * @b: the second integer
 *
 * Return: sum
 */
int mul(int a, int b)
{
int mul;
mul = (a * b);
return (mul);
}
